{"verdict": "As of Xray-core v26.x (verified against tag v26.7.28, released 2026-07-28), XHTTP has exactly three client-selectable modes — packet-up, stream-up, stream-one — set via \"mode\" under xhttpSettings (\"\", \"auto\", \"packet-up\", \"stream-up\", \"stream-one\" are the only accepted values; anything else fails config build with \"unsupported mode\"). There is no stream-two or xstream mode; \"stream-down\" is only an internal log/comment label for the GET download leg, not a configurable mode. Modes requiring cross-request server state: packet-up and stream-up — both carry a random session-id in the URL path, and the server must hold a per-session record (Go sync.Map of httpSession{uploadQueue, isFullyConnected}) between the upload request(s) and the later GET, because the bytes arriving on POST(s) are buffered/reordered (by seq in packet-up) until the GET arrives to drain them; if no GET within 30s the session is reaped. Modes requiring full-duplex: stream-one — one single POST whose request body is the uplink and whose response body is the downlink, read/written concurrently on the same HTTP request (requires H2/H3 end-to-end; HTTP/1.1 cannot do this); stream-up's upload request is also written-to (server sends X-padding keepalive bytes into the POST response every scStreamUpServerSecs while reading the body), though user data stays one-way. A stateless single-request server could serve: stream-one only. One Xray server supports all three simultaneously (server mode \"\"/\"auto\"), because the handler routes purely on method + presence of session-id/seq path segments; server mode \"stream-up\" additionally still accepts stream-one clients.", "findings": [{"claim": "Only packet-up, stream-up and stream-one exist as XHTTP modes in v26.x; no stream-two/xstream mode was ever added.", "evidence": "infra/conf/transport_method.go Build(): switch c.Mode { case \"\": c.Mode=\"auto\"; case \"auto\",\"packet-up\",\"stream-up\",\"stream-one\": default: return nil, errors.New(\"unsupported mode: \" + c.Mode) }. Release notes for v26.3.27 through v26.7.28 contain no new mode names (only XHTTP/3, padding-obfuscation and congestion-control work). \"stream-down\" appears in dialer.go only as a log string for the download leg.", "url": "https://github.com/XTLS/Xray-core/blob/v26.7.28/infra/conf/transport_method.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Auto-mode client selection: packet-up by default; stream-one under REALITY; stream-up if REALITY plus downloadSettings.", "evidence": "transport/internet/splithttp/dialer.go (tag v26.7.28): `if mode == \"\" || mode == \"auto\" { mode = \"packet-up\"; if realityConfig != nil { mode = \"stream-one\"; if transportConfiguration.DownloadSettings != nil { mode = \"stream-up\" } } }`; session-id is generated only when mode != stream-one.", "url": "https://github.com/XTLS/Xray-core/blob/v26.7.28/transport/internet/splithttp/dialer.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Wire shape (defaults): session-id and seq are appended to the URL PATH, not query string; packet-up = N POSTs /path/{session}/{seq} starting at seq 0; stream-up = one streaming POST /path/{session} with no seq; stream-one = a single POST /path/ with no session-id at all; download leg = GET /path/{session}.", "evidence": "transport/internet/splithttp/config.go ApplyMetaToRequest appends sessionId then seqStr to req.URL.Path for PlacementPath (the default); dialer.go skips sessionId generation for stream-one. Discussion #4113 states UUID and seq are placed in the path deliberately, and that stream-one requests /yourpath/ with trailing slash added.", "url": "https://raw.githubusercontent.com/XTLS/Xray-core/v26.7.28/transport/internet/splithttp/config.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Cross-request server state: packet-up and stream-up require a server-side session store keyed by session-id holding an uploadQueue; stream-one stores nothing.", "evidence": "transport/internet/splithttp/hub.go (v26.7.28): `sessions sync.Map`; httpSession{uploadQueue, isFullyConnected}; upsertSession() creates it and a goroutine deletes it after 30s if no GET (`time.Sleep(30 * time.Second)` -> `h.sessions.Delete(sessionId); s.uploadQueue.Close()`); POST handlers Push(Packet{Payload, Seq}) into currentSession.uploadQueue; the GET branch does `defer h.sessions.Delete(sessionId)`; stream-one falls into the `sessionId == \"\"` branch so upsertSession is never called.", "url": "https://github.com/XTLS/Xray-core/blob/v26.7.28/transport/internet/splithttp/hub.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Why state is needed: uploads arrive on HTTP requests distinct from the GET that consumes them; the queue buffers/reorders out-of-order POSTs (max scMaxBufferedPosts, default 30) before the proxy connection reader drains them.", "evidence": "hub.go packet-up branch parses `strconv.ParseUint(seqStr, 10, 64)` then `currentSession.uploadQueue.Push(Packet{Payload: payload, Seq: seq})`; the GET branch sets `conn.reader = currentSession.uploadQueue`. Discussion #4113: multiple POSTs may arrive out of order, server reassembles by seq, buffers up to 30 by default then disconnects.", "url": "https://github.com/XTLS/Xray-core/discussions/4113", "date": "2025-01-01", "confidence": "high"}, {"claim": "Full-duplex: stream-one reads request.Body and writes the response body concurrently on ONE request (server and client); packet-up never needs duplex; stream-up's upload request is read while keepalive padding is written to its response.", "evidence": "hub.go stream-down/stream-one branch: `httpSC := &httpServerConn{Reader: request.Body, ResponseWriter: writer}; conn := splitConn{writer: httpSC, reader: httpSC}; if sessionId != \"\" { // if not stream-one; conn.reader = currentSession.uploadQueue }` — for stream-one the reader IS the request body while writer is the response. stream-up branch spawns a goroutine writing `bytes.Repeat([]byte{'X'}, ...)` into the POST response every scStreamUpServerSecs (default 20-80s) because Cloudflare kills idle downstream connections at 100s.", "url": "https://github.com/XTLS/Xray-core/blob/v26.7.28/transport/internet/splithttp/hub.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Padding: every client request carries random-length padding (default range 100-1000 bytes, cannot be disabled) sent as ?x_padding=... inside the Referer header (legacy) or configurable obfuscated placement; server responses carry an X-Padding header of random length in the same range, and the server validates request padding length.", "evidence": "hub.go: `paddingValue, paddingPlacement := h.config.ExtractXPaddingFromRequest(...)`; `if !h.config.IsPaddingValid(paddingValue, validRange.From, validRange.To, ...) { ... \"invalid padding\" }`; response side ApplyXPaddingToResponse sets Header \"X-Padding\". config.go FillStreamRequest builds PlacementQueryInHeader{Key: \"x_padding\", Header: \"Referer\"} when xPaddingObfsMode is off. infra/conf validation: `if c.XPaddingBytes != (Int32Range{}) && (c.XPaddingBytes.From <= 0 || c.XPaddingBytes.To <= 0) return nil, errors.New(\"xPaddingBytes cannot be disabled\")`.", "url": "https://raw.githubusercontent.com/XTLS/Xray-core/v26.7.28/transport/internet/splithttp/config.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Client-side config delta to switch mode is a single JSON field under xhttpSettings: \"mode\": \"packet-up\" | \"stream-up\" | \"stream-one\" (or omit for auto). Supporting fields gate behavior: noGRPCHeader strips the Content-Type: application/grpc disguise that stream-up/one send by default; scMaxEachPostBytes/scMinPostsIntervalMs only affect packet-up; downloadSettings is required to reach stream-up under REALITY auto and is forbidden in stream-one.", "evidence": "transport_method.go SplitHTTPConfig struct json tags: `Mode string \\`json:\"mode\"\\``, `NoGRPCHeader bool`, etc.; config.go FillStreamRequest: `if request.Body != nil && !c.NoGRPCHeader { // stream-up/one; request.Header.Set(\"Content-Type\", \"application/grpc\") }`; Build(): `if c.Mode == \"stream-one\" { return nil, errors.New(\\`Can not use \"downloadSettings\" in \"stream-one\" mode.\\`) }`.", "url": "https://github.com/XTLS/Xray-core/blob/v26.7.28/infra/conf/transport_method.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Server-side mode gating proves one implementation serves all modes: server mode \"\" or \"auto\" accepts packet-up, stream-up and stream-one; explicit \"stream-up\" STILL accepts stream-one; explicit \"stream-one\" or \"packet-up\" reject the others with 400.", "evidence": "hub.go gates: `if sessionId == \"\" && h.config.Mode != \"\" && h.config.Mode != \"auto\" && h.config.Mode != \"stream-one\" && h.config.Mode != \"stream-up\"` -> 400 \"stream-one mode is not allowed\"; `if seqStr == \"\" && h.config.Mode != \"\" && h.config.Mode != \"auto\" && h.config.Mode != \"stream-up\"` -> 400 \"stream-up mode is not allowed\"; `if h.config.Mode != \"\" && h.config.Mode != \"auto\" && h.config.Mode != \"packet-up\"` -> 400 \"packet-up mode is not allowed\".", "url": "https://raw.githubusercontent.com/XTLS/Xray-core/v26.7.28/transport/internet/splithttp/hub.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "Parameter semantics: scMaxEachPostBytes (Int32Range, default 1000000) caps each packet-up POST body, enforced server-side with 413; scMinPostsIntervalMs (Int32Range, default 30ms) paces packet-up POST dispatch client-side; scMaxBufferedPosts (default 30) is the per-session server buffer depth; scStreamUpServerSecs (default 20-80, -1 disables) is the stream-up keepalive interval; noSSEHeader (server) drops the text/event-stream download disguise header.", "evidence": "splithttp/config.go GetNormalizedScMaxEachPostBytes {From:1000000,To:1000000}, ScMinPostsIntervalMs {30,30}, ScMaxBufferedPosts 30, ScStreamUpServerSecs {20,80}; hub.go: `if request.ContentLength > int64(scMaxEachPostBytes)` -> 413; client dialer.go sleeps `scMinPostsIntervalMs.rand()` minus elapsed between POSTs; download branch sets X-Accel-Buffering: no, Cache-Control: no-store, and Content-Type: text/event-stream `if !h.config.NoSSEHeader`.", "url": "https://raw.githubusercontent.com/XTLS/Xray-core/v26.7.28/transport/internet/splithttp/config.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "xmux parameters govern how many HTTP requests a client multiplexes over reused H2/H3 connections: maxConcurrency/maxConnections (mutually exclusive), cMaxReuseTimes, hMaxRequestTimes, hMaxReusableSecs, hKeepAlivePeriod. Current default when the whole xmux object is absent: maxConnections=3, hMaxRequestTimes=600-900 (Nginx caps ~1000), hMaxReusableSecs=1800-3000 (Nginx caps 1h), hKeepAlivePeriod=0 meaning library default; setting any one field removes defaults for the rest.", "evidence": "transport_method.go Build(): `if c.Xmux.MaxConnections.To > 0 && c.Xmux.MaxConcurrency.To > 0 { error }`; `if c.Xmux == (XmuxConfig{}) { MaxConnections 3/3; HMaxRequestTimes 600/900; HMaxReusableSecs 1800/3000 }`. NOTE this differs from discussion #4113's older table (maxConcurrency 16-32 default) — source at v26.7.28 supersedes it. hKeepAlivePeriod is int64 json field, 0 = default, negative disables.", "url": "https://github.com/XTLS/Xray-core/blob/v26.7.28/infra/conf/transport_method.go", "date": "2026-07-28", "confidence": "high"}, {"claim": "2025-2026 additions beyond the classic parameter set: xPaddingObfsMode/xPaddingKey/xPaddingHeader/xPaddingPlacement(cookie|header|query|queryInHeader)/xPaddingMethod(repeat-x|tokenish); uplinkHTTPMethod (POST default; GET only allowed in packet-up); sessionIDPlacement/sessionIDKey(X-Session|x_session)/sessionIDTable/sessionIDLength; seqPlacement/seqKey(X-Seq|x_seq); uplinkDataPlacement(auto|body|header|cookie — header/cookie only in packet-up)/uplinkDataKey(X-Data)/uplinkChunkSize; serverMaxHeaderBytes (default 8192); 'extra' raw-JSON merge where only host/path/mode override it.", "evidence": "config.proto Config message fields 14-29; transport_method.go Build() validation switches for each placement; `if c.UplinkHTTPMethod == \"GET\" && c.Mode != \"packet-up\"` -> error; Extra handling unmarshals into a fresh SplitHTTPConfig then copies back Host/Path/Mode.", "url": "https://raw.githubusercontent.com/XTLS/Xray-core/v26.7.28/transport/internet/splithttp/config.proto", "date": "2026-07-28", "confidence": "high"}, {"claim": "Cloudflare Workers compatibility: stream-up breaks when the domain is wrapped behind a CF Worker/Pages reverse proxy while packet-up still works (but consumes many requests); plain Cloudflare proxy supports stream-up, and Cloudflare reportedly supports stream-one fully. Issue #4359 (stream-up failing through Cloudflare CDN/Tunnel after commit ca9a902, firstLen=0 / failed to read request version > EOF) was closed as not planned (Feb 2025).", "evidence": "Discussion #4118 user report: wrapping the domain in CF workers/pages reverse proxy breaks stream-up, packet-up works but free quota exhausts; post also says Cloudflare currently perfectly supports stream-one mode. Issue #4359 titled \"xhttp cannot use stream-up through CloudflareCDN\", opened 2025-02-06, closed not planned.", "url": "https://github.com/XTLS/Xray-core/issues/4359", "date": "2025-02-06", "confidence": "medium"}], "caveats": "All mode/wire/state claims above were verified directly against the v26.7.28 tag source (released 2026-07-28, latest release as of research date), which supersedes the prose docs: the official xtls.github.io/config/transports/xhttp.html page and Xray-docs-next equivalent are stubs that just link to GitHub discussion #4113, and some values in that discussion are stale — notably xmux defaults changed from maxConcurrency \"16-32\" (per #4113) to maxConnections 3 in current source, and #4113 predates the 2025-2026 placement-configurability additions (uplinkHTTPMethod, sessionIDTable, xPaddingObfsMethod, etc.). Discussion #4118 dates to Dec 2024 and issue #4359 to Feb 2025 — both pre-v26 and flagged as such; they are used only for the Cloudflare/Workers compatibility claims, which I could not re-verify against a 2026 primary source. The WebSearch tool returned an AI-summarized result containing claims I could not corroborate (e.g. \"mode auto negotiation\"); none of those were relied upon where they conflict with fetched source. Minor caveat: stream-up's upload request does carry a server->client direction (keepalive padding only), so strictly speaking stream-up benefits from a full-duplex-capable hop too, but user data remains unidirectional there; only stream-one moves user data both ways on one request."}

=====TEXT BLOCKS=====

----- block 0 -----
<think>
Now I have the crucial server handler code. Key observations:

1. `isUplinkRequest` branch (POST with sessionId): pushes Packet{Payload, Seq} into `currentSession.uploadQueue`. The uploadQueue is a cross-request buffer per session.
2. GET branch or stream-one (`request.Method == "GET" || sessionId == ""`): 
 - For non-stream-one: marks session fully connected, deletes session after, reads from uploadQueue as the connection reader.
   - For stream-one: `conn.reader = httpSC` — the request body itself is the reader. So stream-one reads request body and writes response concurrently on ONE request = full duplex.

Let me now read lines 150-262 to see the uplink gate and stream-up keepalive logic in detail.

----- block 1 -----
<think>
Now I have the crucial server handler code. Let me now read the GET/download branch (lines 262-400) to confirm how the download works and the stream-one branch.

----- block 2 -----
<think>
Excellent! Now I have the decisive code. This is the key evidence:

Lines 240-347: packet-up branch — reads body (up to scMaxEachPostBytes), parses seq, pushes Packet{Payload, Seq} into `currentSession.uploadQueue` — cross-request state.

Lines 348-399: `} else if request.Method == "GET" || sessionId == "" { // stream-down, stream-one`
- GET: session fully connected, delete after; sets X-Accel-Buffering no, Cache-Control no-store, Content-Type text/event-stream; creates httpServerConn with Reader: request.Body; if sessionId != "" (not stream-one), conn.reader = currentSession.uploadQueue. For stream-one, conn.reader = httpSC = request.Body — so the SAME request's body is read while response is written = full duplex on one request.
- For stream-down (GET): the reader is the uploadQueue fed by prior POSTs — cross-request state.

Now let me read lines 140-240 for padding validation, mode gates, session upsert, and stream-up keepalive loop.
</think>
This is the decisive server code. Let me get lines 140-240 (uplink gate, stream-up keepalive).

----- block 3 -----
<think>
Now I have the decisive code. This is the key evidence:

**Lines 240-347 (packet-up POST branch):**
- Mode gate: `if h.config.Mode != "" && h.config.Mode != "auto" && h.config.Mode != "packet-up"` → reject with400
- Reads body (up to scMaxEachPostBytes), parses seq from path, pushes `Packet{Payload, Seq}` into `currentSession.uploadQueue` — cross-request state- New placement options: uplink data can be in headers (`uplinkDataKey-0`, `-1`, ... base64 RawURL), cookies (`uplinkDataKey_0`...), body, or auto (concatenated)

**Lines 348-399 (GET / stream-one branch):**
- GET: session fully connected, deferred delete of session
- Sets X-Accel-Buffering: no, Cache-Control: no-store, Content-Type: text/event-stream (unless noSSEHeader)
- Creates httpServerConn{Reader: request.Body, ResponseWriter: writer}
- `if sessionId != "" { // if not stream-one; conn.reader = currentSession.uploadQueue }`
- So for stream-one: conn.reader = httpSC = request.Body — the SAME request's body is read while response written = full duplex on one request.
- For stream-down GET: reader = uploadQueue fed by prior POSTs.

Now let me read lines 140-240 for the padding validation, mode gates for stream-one/stream-up, session upsert, and stream-up keepalive.
</think>
This is decisive. Now the uplink branch (140-240).

----- block 4 -----
<think>
Excellent! Now I have the crucial download/stream-one branch. Key findings:

**Lines 240-347 (packet-up POST):**
- Mode gate: reject if mode not auto/packet-up
- New: uplink data can be in headers (`uplinkDataKey-0`, `-1`... base64 RawURL), cookies (`uplinkDataKey_0`...), body, or auto (concatenated header+cookie+body)
- scMaxEachPostBytes size check
- seq parsed from path, pushed into `currentSession.uploadQueue` — cross-request state

**Lines 348-399 (GET / stream-one):**
- GET: session fully connected, deferred delete
- Headers: X-Accel-Buffering: no, Cache-Control: no-store, Content-Type: text/event-stream (unless noSSEHeader)
- httpSC wraps request.Body as Reader + ResponseWriter
- splitConn{writer: httpSC, reader: httpSC}
- `if sessionId != "" { // if not stream-one; conn.reader = currentSession.uploadQueue }`
- So stream-one: reader = request.Body — the SAME request's body is read while response is written = FULL DUPLEX on one HTTP request.
- For stream-down (GET with sessionId): reader = uploadQueue fed by prior POSTs.

Now I need lines 150-239 for the uplink gates, padding validation, session upsert, and stream-up keepalive loop.
</think>
Now the decisive uplink branch.

----- block 5 -----
The sed/awk output was truncated oddly for lines 238+. That range contains the load-bearing GET/stream-one branch, so let me extract it properly with Python.
