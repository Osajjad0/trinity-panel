{"verdict": "Stateless packet-up: IMPOSSIBLE — the forcing invariant is that a packet-up session requires exactly one live outbound TCP connection to the destination that persists across, and accepts ordered writes from, multiple independent HTTP requests (the long-poll GET plus every uplink POST) while returning reads to the downlink request. Neither TCP nor TLS provides any mechanism to transplant a half-lived byte stream into a new connection: a freshly dialed TCP connection presents the destination with a brand-new stream (fresh ISN, independent kernel socket), a TLS endpoint on that connection expects handshake bytes and dies parsing mid-stream application-data records, record sequence numbers/AEAD nonces are per-connection running counters that cannot be adopted mid-flight, and replaying buffered bytes on a redial produces a divergent handshake transcript whose Finished check fails. Therefore the tunnel's continuity must be anchored in something that survives request boundaries and is reachable from every subsequent request — descriptive state in the URL (session UUID, seq, which packet-up already carries) identifies the session but cannot substitute for owning its socket. On Cloudflare Workers that anchor is necessarily a Durable Object ('single-point-of-coordination', 'stateful serverless'): Workers forbid creating TCP sockets in global scope or sharing them across requests, and routing guarantees no two requests land on the same isolate. Stateless-per-request serving is achievable only when the entire tunnel fits inside one request — WebSocket upgrade or XHTTP stream-one — which are different modes, not stateless packet-up; stream-up does not escape either, since a streamed POST plus a separate GET still spans two concurrent requests requiring a join point.", "findings": [{"claim": "XHTTP packet-up is architecturally defined as many POSTs + one GET correlated server-side: the server associates uplink and downlink purely by a random UUID in the path, reorders out-of-order POSTs by seq into a buffer (scMaxBufferedPosts, default 30, disconnect on overflow), and terminates the session if up/down are not associated within 30 seconds.", "evidence": "Client sends 'POST /yourpath/sameUUID/seq' (seq from 0, next POST only after previous body fully sent, without waiting for response); 'multiple POSTs may rarely arrive out of order, the server reassembles by seq, caching at most 30 by default'; downlink is 'GET /yourpath/sameUUID'; the downlink UUID equals the uplink UUID 'so the server can associate [them], if the server fails to associate within 30 seconds, the session is terminated'. This proves the protocol design presumes a stateful server-side rendezvous holding the destination connection and a reorder buffer.", "url": "https://github.com/XTLS/Xray-core/discussions/4113", "date": "2025 (fetch did not expose exact post date)", "confidence": "high"}, {"claim": "Cloudflare Workers cannot hold or share a TCP socket across requests: sockets may only be created inside a handler, never in global scope.", "evidence": "Official docs: 'TCP sockets cannot be created in global scope and shared across requests.' and 'You should always create TCP sockets within a handler (ex: fetch(), scheduled(), queue()) or alarm().' connect() from 'cloudflare:sockets' returns readable/writable streams for one connection; Workers also cannot accept inbound TCP (CONNECT method unsupported), closing off any 'origin calls back in' workaround. In Durable Objects, by contrast, 'an open TCP socket keeps the Durable Object in memory' (up to ~15 minutes per connection) — i.e., cross-request socket ownership is precisely the DO feature set.", "url": "https://developers.cloudflare.com/workers/runtime-apis/tcp-sockets/", "date": "accessed 2026-08-22", "confidence": "high"}, {"claim": "There is no isolate/colo affinity to exploit as soft state: consecutive requests may land on different isolates or machines, and global-scope mutation is officially discouraged.", "evidence": "'Because there is no guarantee that any two user requests will be routed to the same or a different instance of your Worker, Cloudflare recommends you do not use or mutate global state.' Isolates 'may be spun down and evicted' at any time. So even ignoring the socket-in-global-scope ban, a GET and its POSTs have no guaranteed common process to find one another in.", "url": "https://developers.cloudflare.com/workers/reference/how-workers-works/", "date": "accessed 2026-08-22", "confidence": "high"}, {"claim": "Cross-request coordination on Workers is, by Cloudflare's own definition, a stateful construct (Durable Objects) — the platform-sanctioned home for exactly the rendezvous packet-up needs.", "evidence": "DO docs: 'Durable Objects enable stateful serverless applications', they provide 'a single-point-of-coordination between Cloudflare Workers', and 'Because WebSocket sessions are long-lived, applications commonly use Durable Objects' to hold them. Each DO has a globally-unique name so any request worldwide reaches the same instance — the property a stateless Worker lacks and packet-up requires.", "url": "https://developers.cloudflare.com/durable-objects/what-are-durable-objects/", "date": "accessed 2026-08-22", "confidence": "high"}, {"claim": "TLS session resumption does NOT provide stream continuity: it establishes a NEW connection with freshly bootstrapped cryptographic state, shortcutting only the handshake.", "evidence": "RFC 8446 sec 2.2: if the server accepts the PSK, 'the security context of the new connection is cryptographically tied to the original connection' and 'the key derived from the initial handshake is used to bootstrap the cryptographic state instead of a full handshake.' Resumption yields an independent TLS session with reset record counters and no continuation of prior application data; additionally the PSK belongs to the inner client, not the proxying Worker, and the destination still sees a brand-new TCP stream underneath. So per-chunk reconnect-with-resumption reconstructs key establishment, never the logical tunnel.", "url": "https://www.rfc-editor.org/rfc/rfc8446.html", "date": "2018-08", "confidence": "high"}, {"claim": "TLS record encryption uses a per-connection running record counter in the AEAD nonce — continuous per-endpoint state a fresh connection cannot inherit.", "evidence": "'The IV will be modified by XOR'ing it by the count of records that have already been encrypted with this key', incrementing 0,1,2,... across successive records under the same key (resetting only at key epochs). Both endpoints advance this counter in lockstep; splicing mid-stream chunks onto a new TCP connection would present records whose position in the counter sequence is meaningless to a stack that never saw the earlier records — and pre-handshake records on a new connection parse as invalid handshake messages anyway. (Primary text RFC 8446 sec 5.3 could not be fetched due to truncation; pinned via this authoritative TLS 1.3 walkthrough instead.)", "url": "https://tls13.xargs.org/", "date": "accessed 2026-08-22", "confidence": "medium"}, {"claim": "The only Workers-compatible XHTTP shapes are single-request ones: stream-one puts the entire bidirectional tunnel in ONE POST (response is the downlink), which fits the per-request socket model, whereas packet-up (N POSTs + 1 GET) and stream-up (1 streaming POST + 1 GET) inherently span multiple concurrent requests.", "evidence": "Discussion 4113 defines: stream-one = 'POST /yourpath/' with 'the response being the downlink, bidirectional streaming' (one request total); stream-up replaces packet-up's segmented uploads with a single streaming POST to '/yourpath/sameUUID' plus the separate GET downlink (two concurrent requests); packet-up = cyclic POSTs '/sameUUID/seq' plus GET (N+1 requests). Any multi-request shape forces a join point across requests; the join point is the irreducible state.", "url": "https://github.com/XTLS/Xray-core/discussions/4113", "date": "2025", "confidence": "high"}, {"claim": "Plain-HTTP destinations admit only a degenerate stateless variant that stops being a tunnel: fresh-connection-per-chunk works solely when the inner protocol is request-scoped and each chunk is a self-contained transaction — it breaks arbitrary TCP (TLS, SSH, RDP) and inner TLS continuity entirely.", "evidence": "Reasoning from TCP/TCP-adjacent architecture (no normative source asserts an absence): TCP exposes one byte stream per connection with no cross-connection attach/resume primitive; a destination application treats each accept()ed connection as an independent peer. An HTTPS destination receiving a partial ClientHello then FIN fails its handshake; a successor connection beginning with mid-stream application-data records fails record parsing; redial-plus-replay fails because the server's fresh ephemeral contributions change the transcript hash, so the client's Finished verification cannot succeed. Hence 'works for plain HTTP' reduces to a per-chunk reverse proxy, not VLESS stream semantics.", "url": "https://www.rfc-editor.org/rfc/rfc8446.html", "date": "2018-08", "confidence": "medium"}], "caveats": "(1) The canonical XHTTP specification lives in GitHub discussion 4113, whose HTML fetch did not expose the post date; it is known to be the 2025 'XHTTP: Beyond REALITY' document by RPRX, but treat the exact date as unverified. (2) One WebSearch result (an 'XHTTP on Workers + Durable Objects' troubleshooting roundup) contained synthesized-looking specifics with no verifiable URLs; I discarded it entirely and relied only on directly-fetched pages. (3) RFC 8446 Section 5.3 (Per-Record Nonce) could not be quoted from the primary source — both rfc-editor.org and datatracker fetches truncated before Section 5 or errored — so the per-record counter fact is pinned via the tls13.xargs.org walkthrough (medium confidence on wording, high confidence on substance). (4) blog.cloudflare.com/introducing-durable-objects returned 404 on direct fetch (likely bot protection), so its content came via search summary only; the load-bearing DO facts ('stateful serverless', 'single-point-of-coordination') were instead verified against developers.cloudflare.com/durable-objects directly. (5) Two claims rest on protocol architecture rather than quotable normative text, as flagged: TCP's lack of any cross-connection stream-attach primitive (an absence no RFC states), and the destination-side failure modes (partial-handshake FIN, mid-stream-record parse error, transcript-divergence on replay). Both follow deterministically from TCP/RFC 8446 mechanics cited elsewhere in the findings. (6) All Cloudflare documentation quotes reflect the docs as fetched 2026-08-22; the TCP-sockets page notes inbound TCP support is 'coming soon', which, if shipped, would not change the verdict (inbound termination is not cross-request socket sharing)."}

=====TEXT BLOCKS=====

----- block 0 -----
<think>
The user wants me to analyze whether VLESS+XHTTP packet-up can be served statelessly on Cloudflare Workers. This is a research/reasoning task. Let me think about the architecture carefully, and also do some web research to pin down protocol facts.

Let me start by understanding the question deeply:

**VLESS+XHTTP packet-up mode**: The client opens one long-lived GET request (downlink) to receive data from the destination, and sends a series of POST requests (uplink chunks) carrying data toward the destination. All of these belong to ONE logical stream — i.e., one TCP connection from the server (Xray backend) to the destination.

**Question**: Can this be served STATELESSLY on Cloudflare Workers — every HTTP request opening its own fresh outbound TCP connection to the destination, with session state encoded in the request itself (URL/headers), no cross-request memory?

Key constraints:
1. Cloudflare Workers `connect()` API (TCP sockets) — each `connect()` creates a fresh outbound TCP connection. Workers are stateless between requests; global scope exists but is not reliable/shared across isolates/colos. There's no way to hold a socket open across requests... actually, wait — within a single request you can hold a socket for the duration of that request. But a GET long-poll and POSTs are separate requests.

2. The fundamental issue: A TCP connection is identified by 4-tuple + sequence numbers + per-connection state. If each POST opens a FRESH TCP connection to the destination:
   - Plain HTTP destinations: could work if you use HTTP/1.1 keep-alive semantics? No wait — even for plain HTTP, if the client's inner stream is an HTTP session to the destination, each chunk would need to be part of the same HTTP request/response or separate requests. For arbitrary protocols (TLS), the byte stream must be continuous.
   - TLS destinations: TLS record sequence numbers are per-connection monotonically increasing counters used in AEAD nonces. If you splice chunks onto different TCP connections, the inner TLS records still arrive in order IF the bytes are delivered in order — but the problem is: the destination sees N different TCP connections, each starting fresh. The bytes on connection #2 don't continue where connection #1 left off at the TCP level; they start at a new stream. From the destination application's view (e.g., a TLS terminator), each new TCP connection looks like a NEW TLS handshake attempt with garbage/partial data unless...

Wait, let me think again. The key insight: the inner TLS session rides over the outer TCP connection. The destination server (e.g., example.com:443) receives TCP bytes and parses them as a TLS stream. If the Worker opens TCP conn A, writes bytes 0-1000 (part of ClientHello), closes it, then opens TCP conn B and writes bytes 1001-2000, the destination sees:
- Connection A: partial ClientHello, then FIN/RST → TLS handshake fails, connection torn down.
- Connection B: starts mid-stream garbage → invalid TLS record, connection rejected.

So the destination cannot reassemble a coherent TLS stream across multiple TCP connections because TCP provides no cross-connection continuity, and the TLS layer above has no mechanism to say "continue my previous byte stream on this new connection." TLS record sequence numbers are internal state; there's no protocol for "resume byte-stream at offset X" — TLS session resumption only shortcuts the HANDSHAKE (keys), it does not transfer application-data stream position/state. Each resumed session is a NEW TLS connection with fresh sequence numbers, fresh keys, and no memory of previously sent application records.

So for arbitrary TCP/TLS destinations: impossible without holding one TCP connection alive across all the POSTs, which requires either (a) all requests handled by the same Worker invocation (impossible — GET and POSTs are separate HTTP requests), or (b) cross-request shared state pointing at the live socket — which requires Durable Objects (stateful).

Actually, wait — could the Worker hold the socket open in the global scope between requests? Cloudflare Workers global scope persists per-isolate sometimes, but: (1) no guarantee of same isolate handling subsequent requests; (2) sockets tied to request lifetime — actually, Cloudflare's `connect()` socket lifetime: "The socket will be closed when the client disconnects" — sockets can outlive... Let me check. In Workers, `connect()` returns a socket whose lifetime is tied to the request context by default? Actually I recall sockets are tied to the event context (`ctx`), and when the request ends, the socket may be canceled unless kept alive via `ctx.waitUntil`. Even so, another request running in a DIFFERENT isolate (different machine possibly) cannot access that socket object. Global variables are per-isolate, not shared across colos/machines. So cross-request socket sharing requires a single point of serialization = Durable Object (which is exactly what stateful designs use).

Also relevant: Cloudflare explicitly documents that Workers should not maintain state in global scope across requests ("Global scope" / "Request context" docs). And TCP sockets in Workers: https://developers.cloudflare.com/fundamentals/get-started/concepts/workers-runtime/ ... more precisely the `connect()` API docs: developers.cloudflare.com/workers/runtime-apis/tcp-sockets/.

Now, what about the actual Xray XHTTP design? XHTTP packet-up mode: the server-side Xray maintains ONE TCP connection to destination per session; the GET downlink streams responses back; POSTs feed into the same connection. Xray's implementation is inherently stateful server-side. On Cloudflare Workers specifically, people run VLESS+WS or XHTTP through Workers using... hmm, actually there are known projects: "edgetunnel" (cmliu), "VLESS on Workers" using WebSocket (WS supports one WS connection ↔ one TCP connection mapping, which fits Workers' request model perfectly: each WS upgrade request holds a bidirectional stream for its duration). WebSocket works statelessly-per-request because the entire tunnel lives inside ONE request. XHTTP packet-up splits the stream across MULTIPLE requests (GET + many POSTs), which breaks the one-request-one-tunnel invariant.

There's also "splithttp" predecessor. And XHTTP's "mode: auto / packet-up / stream-up / stream-one". stream-up = H2/H3 single request uplink... stream-one = everything in ONE request (both directions multiplexed). Notably, Xray docs mention stream-one works on CDNs that support full duplex; packet-up was designed for CDN compatibility.

Important fact: XHTTP on Cloudflare Workers — known limitation. Actually there were discussions that XHTTP does NOT work well on Cloudflare Workers because Workers can't hold state across requests; people found packet-up fails or needs Durable Objects. Hmm, but actually some projects DO route XHTTP through Workers... Let me research.

Actually let me recall: Xray-sponsor or "XHTTP on CF workers" — I believe the answer documented in the community is that Workers can't do packet-up properly; WebSockets work because of the single-request model. There's also the newer Cloudflare "Durable Objects + WebSockets" pattern.

Let me now analyze the four sub-questions rigorously:

**1. Fresh TCP per uplink POST delivering coherent byte stream?**

No. Reasons:
- TCP: each connection starts at independent ISN; the destination kernel delivers bytes of conn B as a NEW stream. Application reads conn B from byte 0. Bytes written on B are not appended to A's stream at the app layer. There is no "attach to previous stream" primitive in TCP.
- The destination app (TLS terminator or any TCP service) treats each accepted connection as an independent peer. For plain HTTP destinations with keep-alive: each fresh connection could carry a complete HTTP request/response pair — so IF the inner protocol is HTTP AND each logical request fits entirely within one uplink chunk... but that changes the semantics entirely: it becomes a per-chunk request proxy, not a stream tunnel. It only "works" when the inner stream is request-scoped (e.g., simple HTTP GET proxying), which is not the general case.
- Inner TLS over the tunnel: TLS records have per-connection monotonically increasing seqnum embedded implicitly in the AEAD nonce (nonce = static_iv XOR seqnum). The DESTINATION's TLS stack tracks its own recv seqnum per TLS connection. But crucially the TLS connection is bound to the TCP connection underneath — a TLS stack reading from conn B doesn't know about conn A's seqnums; more fundamentally, conn B's first bytes would be interpreted as the START of a TLS handshake. Mid-stream application records arriving on a fresh TCP connection are parsed as handshake messages → fatal decode error alert. So splicing fails at the very first layer.
- Also ordering/race: POSTs can arrive at the Worker out of order (multiple in-flight connections, CDN fan-out). Stateful Xray numbers chunks (XHTTP has sequence/session IDs in headers/path) and buffers to reorder into the single TCP socket. Stateless per-request dialing can't buffer into a socket that isn't there; each POST dials immediately, so out-of-order arrival = out-of-order bytes on distinct connections, unrecoverable.

**2. Any encoding that reconstructs the SAME logical TCP tunnel per request?**

- URL/header state can carry session ID + sequence number — but state description ≠ state realization. Knowing "this is chunk 7 of session S" doesn't give you the live TCP socket for session S. You need a rendezvous: something holding the socket. In Workers, that's a Durable Object (a stateful singleton). Without it, no.
- TLS session resumption: resumption (session tickets/PSK) restores KEYING MATERIAL to skip the handshake, creating a NEW TLS connection with fresh sequence numbers and no continuity of application-data stream. It cannot resume a half-finished application-data stream. So per-chunk reconnect-with-resumption gives you N independent TLS sessions — fine only if the INNER application tolerates each chunk being a whole new TLS session (i.e., inner protocol is request/response per chunk). For arbitrary TCP (e.g., SSH, RDP, an existing HTTPS session with cookies/auth state mid-conversation), no. Note: the client's browser TLS session to the real destination cannot be resumed BY THE WORKER anyway — resumption requires the client's PSK/ticket and produces a new session; the client thinks it has ONE continuous session.
- Multiplexing tricks: e.g., encode chunk index, have each POST replay... no — bytes already consumed by the destination can't be un-consumed. Idempotent replay would require the destination operation to be idempotent; TCP streams aren't.
- What DOES work statelessly: putting the entire tunnel inside one request — WebSocket (upgrade) or XHTTP "stream-one"/H2 duplex, or stream-up (one big upload request while GET... no, stream-up still uses separate GET downlink? Let me check: stream-up = uplink is a single request with Content-Length... Actually XHTTP modes:
  - packet-up: multiple POSTs + one GET
  - stream-up: one POST (uplink streamed) + one GET downlink
  - stream-one: ONE request carrying both directions (full-duplex H2/H3)
  
  stream-up still has TWO concurrent requests needing correlation (the GET and POST share session UUID?) — hmm. In stream-up, the uplink is one streaming POST; downlink is a separate GET. They're correlated by session ID. That still needs the server to join two requests' streams → still cross-request state! Unless... on the server, both requests arrive at the same process. In Xray server, yes it joins them. On Workers, joining two requests again needs a DO. Interesting — so even stream-up needs a stateful join point on CF. stream-one avoids it entirely (single request, bidirectional body) — that WOULD be servable statelessly-ish per request... except the destination socket lives only for that request's lifetime, which is fine since it IS the tunnel. But Cloudflare Workers fetch() response streaming + request body streaming to origin via connect() — yes that works (that's how WS-over-Workers works too).
  
  Actually wait — does Workers support receiving a request body stream and piping to connect() socket and back? Yes — that's the standard pattern (e.g., in vless-on-workers implementations using WS). With plain fetch you can do bidirectional streaming with `duplex: 'half'` limitations... In Workers runtime, you can read request.body ReadableStream and pipe to socket, and return a Response with a ReadableStream from socket. That's full-duplex at the Worker level even though fetch itself is half-duplex — the Worker mediates. Fine.

So the answer to Q2: no encoding reconstructs the same TCP tunnel per request for arbitrary TCP; only single-request encapsulations (WS, stream-one) avoid cross-request state, and those aren't packet-up.

**3. Downlink GET requiring shared state?**

Yes. The GET must return bytes the destination produced in response to uplink data that arrived via OTHER requests. A fresh per-request connection has nothing to read (no request sent → no response bytes) — for HTTP destinations you'd get nothing meaningful; for TLS destinations you can't even complete a handshake without sending the client's ClientHello, which lives in uplink POSTs. Correlating GET with prior POSTs requires a registry mapping session-ID → live socket/buffer = shared mutable state. Additionally the GET must remain open (long-poll/streaming) for the life of the session while OTHER requests mutate the same socket — inherently a shared-resource pattern. Cloudflare docs: state must go in KV/DO/etc.; isolate globals are unreliable and never shared across machines.

**4. Conclusion**: IMPOSSIBLE. Minimal forcing invariant: **exactly-one live outbound TCP connection per logical session must persist across, and be concurrently accessible from, multiple independent HTTP requests** (the GET plus every POST). Equivalently: the tunnel's byte-stream continuity must be anchored to a single long-lived socket whose identity survives request boundaries; HTTP carries no mechanism to transplant a TCP connection's kernel/app state into a new connection, so something must remember and own that socket between requests — that memory is the irreducible cross-request state. (In CF terms: a Durable Object or equivalent singleton.)

Distinguish: plain-HTTP-only destinations degrade to "per-chunk independent HTTP transactions," which works only when each chunk is a self-contained request and responses are polled separately — that's a different protocol (not VLESS stream semantics, breaks inner-TLS, breaks any stateful inner protocol).

Now let me do the web research to pin down facts:
- Xray XHTTP documentation (xtls.github.io) — modes packet-up etc., session/seq headers.
- Cloudflare Workers TCP sockets docs (connect(), lifecycle, no listening, stateless guidance).
- Cloudflare Durable Objects rationale (why cross-request coordination needs DO).
- Maybe community discussions about XHTTP on Workers.
- TLS RFC facts: RFC 8446 §5.3 sequence numbers per-record, §4.2.11 PSK resumption = new connection; maybe cite RFC 9147? QUIC not needed. RFC 8446 for TLS 1.3; RFC 5246 for 1.2 implicit seq nums.

Let me search. I'll batch searches.

Plan searches:
1. site:xtls.github.io XHTTP (fetch the XHTTP doc page)
2. Cloudflare Workers TCP sockets connect() docs
3. Cloudflare Durable Objects "stateful" / why workers stateless docs
4. TLS 1.3 RFC sequence number per record — fetch RFC section? Could cite rfc-editor.org.
5. Community: "xhttp cloudflare workers" github issues/discussions.

Let me do these in parallel batches.
</think>
I'll research this systematically — pinning down the XHTTP protocol spec, Cloudflare Workers' TCP/request model, and the TLS/TCP continuity facts.

----- block 1 -----
<think>
The search results are somewhat summarized (the search tool seems to have generated some content). I should verify the load-bearing facts by fetching the actual docs pages. Let me fetch:
1. https://xtls.github.io/config/transports/xhttp.html — XHTTP modes, session ID/seq headers
2. https://developers.cloudflare.com/workers/runtime-apis/tcp-sockets/ — socket lifecycle
3. Cloudflare Durable Objects intro blog4. RFC 8446 sequence number facts

Let me fetch in parallel.
</think>
Search summaries aren't load-bearing enough on their own. Let me fetch the primary sources to pin down exact protocol and platform facts.

----- block 2 -----
The XHTTP spec is now pinned: server associates up/down by UUID, reorders by `seq` with a buffer, and holds the destination connection. Now the Cloudflare statefulness doctrine and real-world corroboration.

----- block 3 -----
That last search result reads like synthesized speculation, so I'll discard its specifics and corroborate against primary sources only. Two more pins: Cloudflare's runtime model (isolate/global-scope semantics) and the TLS per-record sequence-number fact.

----- block 4 -----
I have all the load-bearing facts pinned from primary sources. Now the reasoning, then the structured output.

**Analysis summary:**

**Q1 — fresh TCP per POST:** No coherent stream is possible. TCP delivers a byte stream *per connection*; a newly dialed connection presents the destination with a brand-new stream (fresh ISN, fresh kernel socket, fresh application reader). There is no TCP primitive to append to or resume another connection's stream — the destination's TLS terminator reading a partial ClientHello followed by FIN fails the handshake, and a follow-up connection opening with mid-stream application-data records parses them as malformed handshake messages and dies. Replaying buffered bytes on a fresh connection fails too: the server contributes fresh ECDHE shares/randoms, so the transcript hash diverges from what the inner client computed, and Finished verification fails. Ordering compounds it: XHTTP's own spec acknowledges POSTs arrive out of order and must be reassembled by `seq` into a buffer feeding one sink — per-request dialing leaves N sinks and no sink to reorder into. Only the degenerate "inner protocol is plain HTTP with self-contained request-scoped chunks" case survives, which is a transaction proxy, not a stream tunnel.

**Q2 — encodings:** Packet-up already encodes descriptive state (UUID + seq in the path, per discussion4113) — but knowing "chunk 7 of session S" doesn't conjure session S's socket; identification is not possession. TLS session resumption (RFC 8446 §2.2) creates a *new* connection with freshly bootstrapped keys and reset per-record counters — it shortens the handshake, it does not continue an application-data stream; moreover the PSK belongs to the inner client, not the Worker, and the destination's TCP layer still sees N independent connections regardless. Replay-based tricks fail on non-idempotency (consumed bytes, advancing record counters). The only constructions that avoid cross-request state are ones where the entire tunnel lives inside one request — WebSocket upgrade or XHTTP stream-one — which are alternatives to packet-up, not stateless versions of it. Stream-up is not exempt: a streamed POST plus a separate GET is still two concurrent requests needing a join point.

**Q3 — downlink GET:** Inherently shared-state. Its response body consists of destination bytes elicited by *other* requests' payloads (for HTTPS you cannot elicit anything without first delivering the uplink handshake bytes), and the spec's 30-second UUID association window is literally a mutable registry of live sessions. Platform-wise: sockets can't be created in global scope or shared across requests, and routing gives zero isolate/colo affinity — the only sanctioned rendezvous is a Durable Object, i.e., stateful by definition.

**Q4 — verdict:** IMPOSSIBLE. The forcing invariant: one live outbound TCP connection per session must persist across and accept ordered writes from multiple independent HTTP requests while feeding reads to the downlink — and since neither TCP nor TLS offers any way to transplant a half-lived stream into a new connection, that socket must be owned by something reachable from every subsequent request. That ownership is irreducible cross-request state (a Durable Object or equivalent singleton).
